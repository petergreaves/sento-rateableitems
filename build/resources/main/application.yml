server:
  port: 7070
spring:
  application:
    name: rateableitemsservice
  profiles:
    active: default
  main:
    allow-bean-definition-overriding: true
  eureka:
    client:
      fetchRegistry: true
      serviceUrl:
        defaultZone: http://localhost:8761/eureka/